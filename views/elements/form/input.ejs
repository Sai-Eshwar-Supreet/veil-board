<%
  const { type = 'text', name, id = name, value = '', required } = locals;
%>

<%- include('./field', {
  ...locals,
  id,
  field: `
    <input
      class="input"
      type="${type}"
      id="${id}"
      name="${name}"
      value="${value}"
      ${required ? 'required' : ''}
    />
  `
}) %>